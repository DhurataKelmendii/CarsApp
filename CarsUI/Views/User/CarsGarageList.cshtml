@model List<Cars.Domain.Entities.CarGarageRelViewModel>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Roboto|Varela+Round">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="~/css/Style.css">
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js"></script>

</head>
<body>
    <div class="container-xl">
        <div class="table-responsive">
            <div class="table-wrapper">
                <div class="table-title">
                    <div class="row">
                        <div class="col-sm-6">
                            <h2>Manage <b>Cars to Garage</b></h2>
                        </div>
                        @*<div class="col-sm-6">
                            <a href="addGarageModal" class="btn btn-success" data-toggle="modal"><i class="material-icons">&#xE147;</i> <span>Add New Car in Garage</span></a>
                        </div>*@
                    </div>
                </div>
                <table class="table table-striped table-hover">
                    <thead>
                        <tr>

                            <th>Car Name</th>
                            <th>Brand</th>
                            <th>ChassisNumber</th>
                            <th>Color</th>
                            <th>EngineType</th>
                            <th>FuelType</th>
                            <th>Price</th>
                            <th>NumberOfSeats</th>
                            <th>Garage Name</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>

                        @foreach (var item in Model)
                        {
                        <tr>
                            <td>@item.Name</td>
                            <td>@item.Brand</td>
                            <td>@item.ChassisNumber</td>
                            <td>@item.Color</td>
                            <td>@item.EngineType</td>
                            <td>@item.FuelType</td>
                            <td>@item.Price</td>
                            <td>@item.NumberOfSeats</td>
                            <td>@item.GarageName</td>

                            <td>
                                <a href="@Url.Action("DeleteCarFromGarage" , "User", new { carId = item.Id, garageId = item.GarageId })"><i class="material-icons">&#xE872;</i></a>
                            </td>
                        </tr>
                          
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>

</body>
</html>
